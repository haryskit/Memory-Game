import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.memorymastergo.com',
  appName: 'Memory Master GO',
  webDir: 'build'
};

export default config;
